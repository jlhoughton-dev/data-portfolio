name: 'data_portfolio'
version: '1.0.0'
config-version: 2

profile: 'data_portfolio'

model-paths: ['models']
analysis-paths: ['analysis']
test-paths: ['tests']
seed-paths: ['seeds']
macro-paths: ['macros']
snapshot-paths: ['snapshots']

target-path: 'target'
clean-targets:
  - 'target'
  - 'dbt_packages'

models:
  data_portfolio:
    +materialized: view
    intermediate:
      +materialized: view
    domain:
      +materialized: table
